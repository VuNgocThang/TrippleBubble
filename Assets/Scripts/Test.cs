using UnityEngine;
using System;
using static Cinemachine.DocumentationSortingAttribute;
using System.Collections.Generic;

public class Test : MonoBehaviour
{
    void InitBubbles()
    {
    //    int count = listIndex.Count;
    //    int countAll = level.bubbles.Count;
    //    int max = level.maxEach;

    //    List<int> listRandom = new List<int>();
    //    int[] arr = new int[count];
    //    while (countAll > 0)
    //    {
    //        int i;
    //        int index;

    //        // Check if the bubble hasChildren = true
    //        bool hasChildrenCondition = /* Your condition to check if bubble hasChildren */;

    //        if (hasChildrenCondition)
    //        {
    //            i = UnityEngine.Random.Range(0, 8); // Random index between 0 and 8
    //            index = listIndex[i];
    //        }
    //        else
    //        {
    //            i = UnityEngine.Random.Range(0, count);
    //            index = listIndex[i];
    //        }

    //        if (arr[i] < max)
    //        {
    //            arr[i] += 3;
    //            for (int y = 0; y < 3; y++)
    //            {
    //                listRandom.Add(index);
    //                countAll--;
    //            }
    //        }   
    //    }

    }

    


   

}
